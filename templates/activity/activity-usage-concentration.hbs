<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "DUST.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "dust-system.activity-usage-notes" }}
    {{/if~}}
</section>
