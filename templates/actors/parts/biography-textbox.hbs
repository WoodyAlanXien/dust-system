<div class="textbox {{ class }}">
    <h3 class="icon">
        {{#if icon}}<i class="{{ icon }}"></i>
        {{~else if svg}}<dust-system-icon src="systems/dust-system/icons/svg/{{ svg }}.svg"></dust-system-icon>{{/if}}
        <span class="roboto-upper">{{ localize label }}</span>
    </h3>
    {{#if @root.editable}}
    <textarea class="untext" name="{{ name }}">{{ value }}</textarea>
    {{else}}
    <p>{{ dust-system-textFormat value }}</p>
    {{/if}}
</div>
