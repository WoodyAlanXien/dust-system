<div class="statblock npc">
    <h4 class="statblock-title">{{#if anchor}}{{{ anchor }}}{{else}}{{ name }}{{/if}}</h4>
    <span class="statblock-tags">{{ summary.tag }}</span>
    <div class="statblock-content">
        <div class="statblock-header">
            <dl>
                <div class="half-width">
                    <dt>{{ localize "DUST.AC" }}</dt>
                    <dd>{{ system.attributes.ac.value }}</dd>
                </div>
                <div class="half-width">
                    <dt>{{ localize "DUST.Initiative" }}</dt>
                    <dd>{{ summary.initiative }}</dd>
                </div>
                <div>
                    <dt>{{ localize "DUST.HP" }}</dt><dd>{{ system.attributes.hp.max }}</dd>
                    {{~#if system.attributes.hp.formula~}}
                    <dd>({{ system.attributes.hp.formula }})</dd>
                    {{~/if~}}
                </div>
                <div>
                    <dt>{{ localize "DUST.Speed" }}</dt>
                    <dd>{{ summary.speed }}</dd>
                </div>
            </dl>
            <div class="abilities">
                {{#each abilityTables}}
                <table>
                    <thead>
                        <tr>
                            <th scope="col" class="visually-hidden">{{ localize "DUST.Ability" }}</th>
                            <th scope="col" class="visually-hidden">{{ localize "DUST.AbilityScoreShort" }}</th>
                            <th scope="col">{{ localize "DUST.AbilityModifierShort" }}</th>
                            <th scope="col">{{ localize "DUST.SavingThrowShort" }}</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each abilities}}
                        <tr>
                            <th scope="row">{{ label }}</th>
                            <td class="score">{{ dust-system-numberFormat value }}</td>
                            <td>{{ dust-system-numberFormat mod signDisplay="always" }}</td>
                            <td>{{ dust-system-numberFormat save.value signDisplay="always" }}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                {{/each}}
            </div>
            <dl>
                {{#if summary.skills}}
                <div>
                    <dt>{{ localize "DUST.Skills" }}</dt>
                    <dd>{{ summary.skills }}</dd>
                </div>
                {{/if}}
                {{#if summary.gear}}
                <div>
                    <dt>{{ localize "DUST.Gear" }}</dt>
                    <dd>{{ summary.gear }}</dd>
                </div>
                {{/if}}
                {{#if summary.vulnerabilities}}
                <div>
                    <dt>{{ localize "DUST.Vulnerabilities" }}</dt>
                    <dd>{{ summary.vulnerabilities }}</dd>
                </div>
                {{/if}}
                {{#if summary.resistances}}
                <div>
                    <dt>{{ localize "DUST.Resistances" }}</dt>
                    <dd>{{ summary.resistances }}</dd>
                </div>
                {{/if}}
                {{#if summary.immunities}}
                <div>
                    <dt>{{ localize "DUST.Immunities" }}</dt>
                    <dd>{{ summary.immunities }}</dd>
                </div>
                {{/if}}
                <div>
                    <dt>{{ localize "DUST.Senses" }}</dt>
                    <dd>{{ summary.senses }}</dd>
                </div>
                <div>
                    <dt>{{ localize "DUST.Languages" }}</dt>
                    <dd>{{ summary.languages }}</dd>
                </div>
                <div>
                    <dt>{{ localize "DUST.AbbreviationCR" }}</dt>
                    <dd>{{ summary.cr }}</dd>
                </div>
            </dl>
        </div>
        {{#each actionSections}}
        <div class="statblock-actions {{ @key }}">
            <h5 class="statblock-actions-title">{{ label }}</h5>
            {{{ description }}}
            {{#each actions}}
            <div class="statblock-action">
                {{{ openingTag }}}
                <span class="name">{{ name }}</span>
                {{{ description }}}
            </div>
            {{/each}}
        </div>
        {{/each}}
    </div>
</div>
